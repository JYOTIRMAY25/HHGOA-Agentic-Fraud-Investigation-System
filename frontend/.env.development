# Development environment for the HHGOA frontend.
# Vite proxies /investigate and /health to the local backend (localhost:8000).
# When VITE_API_BASE_URL is unset, the client falls back to relative paths.